import { NgxMatPaginatorDirective } from './ngx-mat-paginator.directive';

describe('NgxMatPaginatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxMatPaginatorDirective();
    expect(directive).toBeTruthy();
  });
});
